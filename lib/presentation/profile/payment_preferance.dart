import 'package:flutter/material.dart';

class PaymentPreferanceSection extends StatelessWidget {
  const PaymentPreferanceSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
